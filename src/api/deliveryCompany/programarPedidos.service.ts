import { env } from '@/config/env';
import axios from 'axios';

export function login(user: string, pass: string) {
    // return axios.post(`${env.eticos.urlBaseApi}/login`,
    //     { user, pass },
    //     {
    //         headers: {
    //             'Content-Type': 'application/json',
    //         },
    //     }
    // );
}